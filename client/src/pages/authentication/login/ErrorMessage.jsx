import React from "react";

const ErrorMessage = ({ message }) => {
  return <p className="text-red-500 font-semibold text-base -mt-3 mb-2 text-center">{message}</p>;
};

export default ErrorMessage;
